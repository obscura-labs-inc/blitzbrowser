services:
  blitzbrowser:
    build:
      context: ./blitzbrowser 
      dockerfile: Dockerfile
    ports:
      - "9999:9999"
    volumes:
      - ./blitzbrowser-data:/blitzbrowser
    environment:
      API_KEY: asd
      # S3_ENDPOINT: http://s3:9000
      # S3_ACCESS_KEY_ID: rustfsadmin
      # S3_SECRET_ACCESS_KEY: rustfsadmin
      # S3_USER_DATA_BUCKET: user-data
      MAX_BROWSER_INSTANCES: 12
    shm_size: "2gb"
    restart: always
  dashboard:
    build:
      context: ./dashboard 
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      AUTH_KEY: 123
      BLITZBROWSER_API_KEY: asd
    restart: always
  s3:
    image: rustfs/rustfs
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      RUSTFS_VOLUMES: /data
      RUSTFS_ADDRESS: :9000
      RUSTFS_ACCESS_KEY: rustfsadmin
      RUSTFS_SECRET_KEY: rustfsadmin
      RUSTFS_CONSOLE_ENABLE: true
    restart: always
    volumes:
      - s3_data:/data
  # RustFS volume permissions fixer service
  volume-permission-helper:
    image: alpine
    volumes:
      - s3_data:/data
    command: >
      sh -c "
        chown -R 10001:10001 /data &&
        echo 'Volume Permissions fixed' &&
        exit 0
      "
    restart: "no"
volumes:
  s3_data: